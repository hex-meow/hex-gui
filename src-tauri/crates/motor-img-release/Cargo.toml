[package]
name = "motor-img-release"
version = "0.1.0"
edition = "2021"
rust-version = "1.81"
description = "HTTPS release bindings for supplier Motor IMG artifacts"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
