{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: lets the main window call our motor commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
